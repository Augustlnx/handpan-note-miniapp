简谱数字「头上的点（升八度）」和「底端的点（降八度）」的核心逻辑分为**语法解析**、**数据存储**、**视觉渲染**三个阶段，以下是详细拆解：

### 一、语法规则定义（约定八度升降的符号）
首先通过文档定义了八度升降的语法规则：
- `^` 表示**升八度**（对应音符头上的点），`_` 表示**降八度**（对应音符底端的点）；
- 符号数量表示倍数（如 `^^` 是升两个八度，`__` 是降两个八度）；
- 示例：`1^`（1头上1个点）、`2__`（2底端2个点）。

相关代码/文档：
```markdown
# 语法教程 - 八度升降
- `^` 表示升八度，`_` 表示降八度，数量表示倍数。
- 示例：1^ 2_ 3^^ 4__
```

### 二、语法解析（识别符号并转换为数值）
在 `template-parser.ts` 的 `parseNote` 方法中，完成对八度升降符号的解析，核心逻辑：

#### 1. 正则捕获八度符号
通过正则表达式 `(?<octave>[\^_]*)` 捕获音符后的 `^`/`_` 符号：
```typescript
const regex = /(?<leftBracket>\()?(?<accidental>[#b]{0,})(?<note>\d|-)(?<duration>\/(2|4|8|16|32))?(?<delay>\.)?(?<octave>[\^_]*)?(?<multinote>(-.*){0,})?(?<rightBracket>\))?/;
const match = noteData.match(regex);
const { octave } = match.groups;
```

#### 2. 计算八度偏移量（octaveCount）
统计 `^`（升八度）和 `_`（降八度）的数量，转换为数值（正=升、负=降）：
```typescript
if (octave) {
  const upOctave = (octave.match(/\^/g) || []).length; // 统计^的数量
  const downOctave = (octave.match(/_/g) || []).length; // 统计_的数量
  octaveCount = upOctave - downOctave; // 正=升八度，负=降八度
}
```

#### 3. 存储解析结果
将 `octaveCount` 存入 `SNNoteOptions`，传递给音符渲染组件：
```typescript
return {
  noteData,
  octaveCount, // 核心数值：如1=升1八度，-2=降2八度
  // 其他属性...
};
```

### 三、视觉渲染（绘制头上/底端的点）
在 `note.ts` 的 `SNNote` 类中，通过 `drawOctaveCount` 方法绘制八度点，核心逻辑：

#### 1. 核心参数配置
定义点的半径、间距、上下偏移（适配视觉效果）：
```typescript
drawOctaveCount(options: {
  x: number;
  y: number;
  count?: number; // 八度偏移量（octaveCount）
  r?: number; // 点的半径，默认1.5px
  gap?: number; // 多个点的间距，默认5px
  upOffset?: number; // 升八度（头上）的Y偏移，默认18px
  downOffset?: number; // 降八度（底端）的Y偏移，默认5px
}) {
  const {
    x, y, count = this.octaveCount,
    r = 1.5, gap = 5,
    upOffset = 18, downOffset = 5
  } = options;
```

#### 2. 确定绘制方向和基准坐标
- 正数（升八度）：基准Y在音符**上方**（`y - upOffset`）；
- 负数（降八度）：基准Y在音符**底端**（`y + downOffset`）；
```typescript
const absCount = Math.abs(count);
const isUp = count > 0; // 正=升（头上），负=降（底端）
const baseX = x; // 点的X坐标（与音符中心对齐）
const baseY = y + (isUp ? -upOffset : downOffset); // 基准Y坐标
```

#### 3. 循环绘制点
根据 `octaveCount` 的绝对值，绘制对应数量的圆点（SVG的`circle`元素）：
```typescript
for (let i = 0; i < absCount; i++) {
  const yOffset = isUp ? -i * gap : i * gap; // 多个点的Y偏移（垂直排列）
  const circle = document.createElementNS('http://www.w3.org/2000/svg', 'circle');
  circle.setAttribute('cx', `${baseX}`); // X坐标（居中）
  circle.setAttribute('cy', `${baseY + yOffset}`); // Y坐标（按间距偏移）
  circle.setAttribute('r', `${r}`); // 半径
  circle.setAttribute('fill', 'black'); // 颜色
  this.el.appendChild(circle); // 添加到音符的SVG容器中
}
```

### 四、特殊场景适配（装饰音的八度点）
装饰音的八度点渲染逻辑与普通音符一致，但会调整参数（更小的半径、间距）以适配装饰音的视觉尺寸：
```typescript
// 装饰音的八度点绘制（note.ts -> drawGraceNote）
this.drawOctaveCount({
  x: graceNoteX + 3,
  y: graceNoteY,
  count: graceNote.octaveCount,
  r: 1, // 更小的半径
  gap: 4, // 更小的间距
  upOffset: 12,
  downOffset: 3,
});
```

### 核心流程总结
1. **输入**：用户编写带 `^`/`_` 的简谱文本（如 `1^`）；
2. **解析**：正则捕获符号 → 计算 `octaveCount`（如 `1^` → `octaveCount=1`）；
3. **渲染**：
   - 升八度（`octaveCount>0`）：在音符**头上**绘制对应数量的黑点；
   - 降八度（`octaveCount<0`）：在音符**底端**绘制对应数量的黑点；
   - 多个点按固定间距垂直排列，保证视觉规范。

该实现的核心是「符号→数值→视觉」的映射，既遵循简谱的语法规则，又通过SVG元素的精准定位实现了标准化的视觉渲染。