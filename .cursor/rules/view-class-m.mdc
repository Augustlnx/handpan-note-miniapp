---
alwaysApply: true
---


# 乐谱存储数据代码规则


## 基础语法
模块定义
**基本格式：**
```latex
\begin{module}{A-1}
...乐谱内容...
\end{module}
```

**支持可选备注：**
```latex
\begin{module}{A-1}{♪速度}
...乐谱内容...
\end{module}
```

**支持排版样式（可选第三个参数）：**
```latex
\begin{module}{A-1}{备注}{h:180,f:32,s:70}
...乐谱内容...
\end{module}
```
**样式参数说明：**
*   **h:** 小节高度 (默认 160rpx)
*   **f:** 音符字号 (默认 28rpx)
*   **s:** 行间距 (默认 65rpx)

### 行内注记
在行末用 `/*"位置:文字"*/` 添加注记：
```latex
[(5)/(1)|(6)/(2)|...]/*"1:渐强","5:重音"*/\\
```
*   **说明：** 位置从 1 开始，按 16 分音符计数。注记显示在对应音符上方。

### 分隔符
*   **换行：** `\\` （分隔不同视觉行）
*   **小节：** `[ ]` （`][` 为共用小节线）
*   **拍位：** `|` （分隔拍）
*   **细分：** `+` （必须用于分隔音符列）
*   **音符包裹：** `<>` 或 `{}` （将特殊音符包裹为整体）
*   **注释：** `%` （行注释，会被忽略）

### 占位拍标记
在行末用 `#数字` 标记该行末尾有几个占位拍：
```latex
[谱面数据]#2\\
```
*   **说明：** `#2` 表示该行末尾有2个占位拍。占位拍用于保持行宽度一致，但不显示内容且不参与播放。
*   **适用场景：** 删除拍位后保持布局不变形。

---

## 音符格式

### 标准格式：(右手)/(左手)
四轨道系统从上到下依次为：
1.  **Slot 0:** 右手外侧（离中轴远）
2.  **Slot 1:** 右手内侧（贴近中轴）
3.  **Slot 2:** 左手内侧（贴近中轴）
4.  **Slot 3:** 左手外侧（离中轴远）

### 常用格式
*   `(7,5)/(3,1)` → 完整四音符
*   `(5)/(1,2)` → 右手单音符自动贴中轴
*   `(5,3)/(2)` → 左手单音符自动贴中轴
*   `()/(1,2)` → 仅左手演奏
*   `(7,5)/()` → 仅右手演奏
*   `-` 或 `()/()` → 空音符（占位）

### 简写规则（奇数右手，偶数左手）
*   `(1,2,3)` 自动转为 `(3,1)/(2)`：括号前半（右手）按奇数降序，括号后半（左手）按偶数升序。
*   单个数字也可省略括号/斜杠：`1` 解析为 `(1)/()`，`2` 解析为 `()/(2)`。
*   **示例：** `[(1,2,3)+-+-+-]` 将被识别为 `[(3,1)/(2)+-+-+-]`；`[(2+1+-+-]` 将被识别为 `[()/(2)+(1)/()+-+-]`。

### 高级简写（隐含 + 号与手指定）
*   **多位数：** 用 `<>` 或 `{}` 包裹以防混淆。例如 `<12>--` 被识别为 `<12>+-+-`，而不是 `1+2+-+-`。
*   **特殊音符：** 包含修饰符（`'` `,` `_` `^{...}`）的音符需用 `<>` 包裹，如 `<1'^{H}>`。
*   **单手指定：** 用 `"数字/"` 和 `"/数字"` 格式手动指定左右手。例如 `1/` 表示 `()/(1)`，`/1` 表示 `(1)/()`。
*   **完整手指定：** 简写为 `4/1` 等价于 `(4)/(1)`，也可用 `<4/1>` 规范为一个音符位。
*   **所有手指定均支持 `<>`：** `<12/>` 表示 `()/('<12>')`，`/<12>` 表示 `('<12>')/()`。

---

##  示例代码

### 1 简单示例（3/4拍）

\begin{module}{A-1}
% 第一行
[ (7,5)/(3,1) + (6)/(2) |
  (8,6)/(4,3) + (5)/(1,2) |
  (9)/(5) + - |
  (T,8)/(6,5) + (7)/(4) ] \\
% 第二行
[ (5)/(1,2) + (6)/(2) |
  (7,5)/(3,1) + - |
  (8)/(4,2) + (9)/(5) |
  - + (T)/(6) ]
\end{module}

### 2 占位拍示例

\begin{module}{B-1}
% 正常的4拍小节
[ (5)/(1) + - | (6)/(2) + - | (7)/(3) + - | (8)/(4) + - ] \\
% 删除了2拍后，用#2标记保持行宽
[ (5)/(1) + - | (6)/(2) + - ]#2
\end{module}




##  使用技巧
*   空格会被自动忽略。
*   小节线支持 `][` 连写。
*   可使用 `%` 添加注释说明。
*   建议先用简单示例测试。


## 简谱扩展语法
简谱模式下可使用以下扩展标记：

### 八度标记
*   `'` (单引号) → 高八度，音符上方加点。
*   `''` → 高两个八度，音符上方加两点。
*   `,` (逗号) → 低八度，音符下方加点。
*   `,,` → 低两个八度，音符下方加两点。
*   **示例：** `1'` (高八度 do), `5,,` (低两个八度 sol)。

### 时值标记
*   `_` (下划线) → 时值减半，音符添加下划线。
*   **示例：** `1_` (八分音符), `1'_` (高八度八分音符)。

### 上标标记
*   `^{内容}` → 在音符旁添加上标。
*   **左上标：** `^{H}1` → H 显示在 1 的左上方。
*   **右上标：** `1^{H}` → H 显示在 1 的右上方。
*   **双上标：** `^{L}1^{R}` → L 左上，R 右上。
*   **上标音高：** 上标内也支持 `'` 和 `,`，如 `^{1'}`。

### 复杂示例
```latex
^{1''}2,,_^{H}
```
**解析：** 左上标高两个八度的 1，低两个八度带下划线的 2，右上标 H。